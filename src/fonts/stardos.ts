import { Stardos_Stencil } from 'next/font/google';

// * ---------------------------------------------------------------------- * //

const stardos = Stardos_Stencil({
  weight: ['400', '700'],
  subsets: ['latin']
});

// * ---------------------------------------------------------------------- * //

export { stardos };
