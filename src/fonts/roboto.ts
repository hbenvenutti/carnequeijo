import { Roboto } from '@next/font/google';

// * ---------------------------------------------------------------------- * //

const roboto = Roboto({ weight: [ '400', '700' ], subsets: [ 'latin' ] });

// * ---------------------------------------------------------------------- * //

export { roboto };
