import localFont from '@next/font/local';

// * ---------------------------------------------------------------------- * //

const stencilSTD = localFont({ src: './stencil-std-bold.otf' });

// * ---------------------------------------------------------------------- * //

export { stencilSTD };
